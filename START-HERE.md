# Start Here

1. Extract the repository.
2. Double-click `Run-OneClick.bat`.
3. Wait for the report to finish.
4. Review the exit code and output folder shown by the tool.

The launcher runs `Find-WindowsLargeFiles.ps1` directly. There is no menu and the script does not modify files.
